[project]
name = "mcp-server-example"
version = "0.1.0"
description = "EAP-Core scaffolded MCP server"
requires-python = ">=3.11"
dependencies = [
    "eap-core[mcp]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
